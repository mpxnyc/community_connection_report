

::: {#def-observational-unit .theorem}
# Observational unit

An observational unit is a set of random variables in a model that originate from the same data source, typically representing a single individual in epidemiologic studies. Observational units partition the model’s variables, so that the union of all units equals the full set of random variables in the model.

:::